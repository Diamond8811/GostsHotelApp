<Page x:Class="GostsHotelApp.Pages.CatalogPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GostsHotelApp.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Каталог номеров" Background="#0F0F0F">
    <Page.Resources>
        <local:StatusColorConverter x:Key="StatusColorConverter"/>
        <local:CanBookConverter x:Key="CanBookConverter"/>
    </Page.Resources>
    <ScrollViewer>
        <StackPanel Margin="10">
            <TextBlock Text="👻 Доступные номера" FontSize="28" FontWeight="Bold" Foreground="#708090" Margin="0,0,0,20"/>
            <ItemsControl x:Name="roomsList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#1E1E1E" BorderBrush="#708090" BorderThickness="1" Margin="10" Padding="10" Width="250">
                            <StackPanel>
                                <TextBlock Text="🛌" FontSize="48" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding RoomNumber}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding RoomTypes.TypeName}" FontSize="14" HorizontalAlignment="Center" Foreground="#708090"/>
                                <TextBlock Text="{Binding PricePerNight}" FontSize="12" HorizontalAlignment="Center" Margin="0,5"/>
                                <TextBlock Text="Статус:" FontSize="12"/>
                                <TextBlock Text="{Binding Status}" FontSize="12" Foreground="{Binding Status, Converter={StaticResource StatusColorConverter}}"/>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
                                    <Button Content="Подробнее" Tag="{Binding RoomID}" Click="Details_Click"/>
                                    <Button Content="Забронировать" Tag="{Binding RoomID}" Click="Book_Click" IsEnabled="{Binding Status, Converter={StaticResource CanBookConverter}}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</Page>