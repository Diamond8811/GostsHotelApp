<Window x:Class="GostsHotelApp.Windows.RoomDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GostsHotelApp.Windows"
        mc:Ignorable="d"
        Title="Детали номера" Height="500" Width="600" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="tbRoomNumber" FontSize="24" FontWeight="Bold" Foreground="#708090" Margin="0,0,0,10"/>
        <TextBlock Grid.Row="1" Text="Тип:"/>
        <TextBlock x:Name="tbType" Grid.Row="2" Margin="0,5"/>
        <TextBlock Grid.Row="3" Text="Описание типа:"/>
        <TextBlock x:Name="tbDescription" Grid.Row="4" TextWrapping="Wrap" Margin="0,5"/>
        <TextBlock Grid.Row="5" Text="Услуги, добавленные к этому номеру (история):" FontWeight="Bold" Margin="0,10,0,5"/>
        <DataGrid x:Name="dgServices" Grid.Row="6" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn  Header="Услуга" Binding="{Binding ServiceName}"/>
                <DataGridTextColumn  Header="Цена" Binding="{Binding Price}"/>
                <DataGridTextColumn Header="Единица" Binding="{Binding Unit}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
